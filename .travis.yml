language: node_js

node_js:
    - "node"
    - "12"
    - "14"

sudo: false

dist: xenial
services:
  - xvfb

addons:
  firefox: "49.0"

git:
    submodules: false


before_script:
    - npm install
    - npm install -g @angular/cli



cache:
  directories:
     - ./node_modules


script:
    # - karma start --browsers Firefox --single-run
    - npm test --no-watch --no-progress --single-run
    # - npm test --browsers=ChromeHeadlessCI
    # - ./node_modules/.bin/karma start --browsers Firefox --single-run

after_success:
    - npm run report-coveralls
    - npm run report-codecov
